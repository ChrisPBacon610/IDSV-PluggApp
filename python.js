window.PYTASKS=[{"g":"Input and output","fn":"input()","t":"Reading what the user types","teach":"<code>input()</code> prints its argument, waits for the user to type a line and press Enter, and gives you back that line <strong>as a string</strong> — always a string, even if they typed digits.","code":"name = input(\"What is your name? \")\nprint(\"Hello\", name)","task":"Ask for a city and print <code>You live in &lt;city&gt;</code>.","sol":"city = input(\"Which city do you live in? \")\nprint(\"You live in\", city)"},{"g":"Input and output","fn":"int() float()","t":"Turning text into a number","teach":"Input is text, so <code>\"3\" + \"4\"</code> gives <code>\"34\"</code>, not 7. Wrap it in <code>int()</code> for whole numbers or <code>float()</code> for decimals before doing arithmetic.","code":"grade = int(input(\"Grade (1-5): \"))\ntotal = grade + 10\nprint(total)","task":"Read two numbers and print their sum.","sol":"a = int(input(\"First number: \"))\nb = int(input(\"Second number: \"))\nprint(a + b)","note":"int(\"4.5\") raises an error — use float() when the text has a decimal point."},{"g":"Input and output","fn":"print()","t":"Printing several things at once","teach":"<code>print()</code> takes any number of values, separated by commas, and puts a space between them. Use <code>sep=</code> to change that separator and <code>end=</code> to change what it prints at the end instead of a newline.","code":"print(\"Speed:\", 42, \"km/h\")\nprint(\"a\", \"b\", \"c\", sep=\"-\")\nprint(\"no newline\", end=\"\")","task":"Print <code>Factor: content</code> using one print with a comma.","sol":"print(\"Factor:\", \"content\")"},{"g":"Input and output","fn":"f-strings","t":"Putting values inside text","teach":"Put <code>f</code> before the quotes and any expression inside <code>{ }</code> is replaced by its value. This is the tidiest way to build a line of output.","code":"name = \"Ada\"\nscore = 4.5\nprint(f\"{name} scored {score}\")\nprint(f\"Average: {score:.2f}\")","task":"Given <code>factor = \"teaching\"</code> and <code>avg = 3.6666</code>, print <code>teaching: 3.67</code>.","sol":"factor = \"teaching\"\navg = 3.6666\nprint(f\"{factor}: {avg:.2f}\")","note":"{avg:.2f} rounds to two decimals. The exam task says you do NOT need to tidy the decimals — plain {avg} is accepted there."},{"g":"Making decisions","fn":"if / elif / else","t":"Choosing what to run","teach":"<code>if</code> runs its indented block when the condition is true. <code>elif</code> tests another condition, <code>else</code> catches everything left. The indentation is what marks the block — Python has no braces.","code":"grade = 3\nif grade >= 5:\n    print(\"Excellent\")\nelif grade >= 3:\n    print(\"Pass\")\nelse:\n    print(\"Fail\")","task":"Read a number and print whether it is positive, negative or zero.","sol":"n = int(input(\"Number: \"))\nif n > 0:\n    print(\"positive\")\nelif n < 0:\n    print(\"negative\")\nelse:\n    print(\"zero\")"},{"g":"Making decisions","fn":"== and in","t":"Comparing, and testing membership","teach":"<code>==</code> asks whether two values are equal (<code>=</code> assigns, <code>==</code> compares). <code>in</code> asks whether a value appears in a list or a string — exactly what you need to check whether a name has answered already.","code":"answered = [\"ada\", \"alan\"]\nname = \"ada\"\nif name in answered:\n    print(\"Har redan svarat!\")\nelse:\n    answered.append(name)","task":"Given <code>names = [\"lisa\", \"omar\"]</code>, print <code>known</code> if <code>\"omar\"</code> is in it, otherwise <code>new</code>.","sol":"names = [\"lisa\", \"omar\"]\nif \"omar\" in names:\n    print(\"known\")\nelse:\n    print(\"new\")","note":"This is the membership check the exam's survey task is built around."},{"g":"Repeating things","fn":"while","t":"Looping while something is true","teach":"<code>while</code> repeats its block for as long as the condition holds. Something inside the loop must eventually make the condition false, or it never stops.","code":"count = 3\nwhile count > 0:\n    print(count)\n    count = count - 1\nprint(\"Go\")","task":"Print the numbers 1 to 5 using a while loop.","sol":"n = 1\nwhile n <= 5:\n    print(n)\n    n = n + 1"},{"g":"Repeating things","fn":"while True / break","t":"Looping until a sentinel word","teach":"When you do not know how many times to loop, run forever with <code>while True</code> and leave with <code>break</code> when the user types the stop word. This is the shape the exam's programming task asks for.","code":"while True:\n    name = input(\"Name: \")\n    if name == \"sluta\":\n        break\n    print(\"Hello\", name)\nprint(\"Done\")","task":"Keep asking for words until the user types <code>quit</code>, then print how many words were given.","sol":"count = 0\nwhile True:\n    word = input(\"Word: \")\n    if word == \"quit\":\n        break\n    count = count + 1\nprint(\"You gave\", count, \"words\")","note":"Check the sentinel BEFORE using the value, or the stop word gets counted as data."},{"g":"Repeating things","fn":"for / range()","t":"Looping a known number of times","teach":"<code>for</code> walks through a sequence. <code>range(n)</code> gives 0, 1, … n−1, and <code>range(a, b)</code> gives a up to b−1. Use <code>for</code> when you know how many steps; <code>while</code> when you do not.","code":"for i in range(3):\n    print(\"round\", i)\n\nfor factor in [\"content\", \"pace\"]:\n    print(factor)","task":"Print the four factor names from a list, one per line.","sol":"factors = [\"content\", \"pace\", \"teaching\", \"material\"]\nfor factor in factors:\n    print(factor)"},{"g":"Lists","fn":"[ ] and append()","t":"Collecting values","teach":"A list holds values in order. Index from <code>0</code>, and add to the end with <code>.append()</code>. <code>len()</code> gives how many items it holds.","code":"names = []\nnames.append(\"ada\")\nnames.append(\"alan\")\nprint(names[0])\nprint(len(names))","task":"Build a list of three numbers read from the user, then print the list.","sol":"numbers = []\nfor i in range(3):\n    numbers.append(int(input(\"Number: \")))\nprint(numbers)"},{"g":"Lists","fn":"sum() / len()","t":"Totals and averages","teach":"<code>sum()</code> adds a list of numbers and <code>len()</code> counts them, so the average is <code>sum(x) / len(x)</code>. Guard against an empty list — dividing by zero is an error.","code":"scores = [4, 5, 3]\nprint(sum(scores))\nprint(sum(scores) / len(scores))","task":"Print the average of a list, or <code>no answers</code> when the list is empty.","sol":"scores = []\nif len(scores) == 0:\n    print(\"no answers\")\nelse:\n    print(sum(scores) / len(scores))","note":"The exam task needs exactly this guard: \"Ingen besvarade enkäten!\" when nobody answered."},{"g":"Lists","fn":"parallel lists","t":"Keeping a running total per factor","teach":"You can keep one list of names and a second list of totals in the same order, so position <code>i</code> in both refers to the same factor. Add to a position with <code>totals[i] = totals[i] + value</code>.","code":"factors = [\"content\", \"pace\"]\ntotals = [0, 0]\ntotals[0] = totals[0] + 4\ntotals[1] = totals[1] + 5\nprint(totals)","task":"Given <code>factors</code> and <code>totals = [0, 0]</code>, ask for a grade for each factor and add it to its total.","sol":"factors = [\"content\", \"pace\"]\ntotals = [0, 0]\nfor i in range(len(factors)):\n    grade = int(input(f\"Ange betyg (1-5) för {factors[i]}: \"))\n    totals[i] = totals[i] + grade\nprint(totals)"},{"g":"Lists","fn":"enumerate()","t":"Index and value together","teach":"<code>enumerate()</code> hands you the position and the item at the same time, so you do not need <code>range(len(...))</code> and an index lookup.","code":"factors = [\"content\", \"pace\"]\nfor i, factor in enumerate(factors):\n    print(i, factor)","task":"Print each factor as <code>1. content</code>, <code>2. pace</code> … numbering from 1.","sol":"factors = [\"content\", \"pace\", \"teaching\"]\nfor i, factor in enumerate(factors):\n    print(f\"{i + 1}. {factor}\")"},{"g":"Dictionaries","fn":"{ } and keys","t":"Looking something up by name","teach":"A dictionary stores values under keys instead of positions. Read with <code>d[key]</code>, write with <code>d[key] = value</code>, and test with <code>key in d</code>.","code":"totals = {\"content\": 0, \"pace\": 0}\ntotals[\"content\"] = totals[\"content\"] + 4\nprint(totals[\"content\"])\nprint(\"pace\" in totals)","task":"Start an empty dictionary and count how many times each word in a list appears.","sol":"words = [\"a\", \"b\", \"a\"]\ncounts = {}\nfor w in words:\n    if w in counts:\n        counts[w] = counts[w] + 1\n    else:\n        counts[w] = 1\nprint(counts)"},{"g":"Dictionaries","fn":"get()","t":"A default instead of an error","teach":"<code>d[key]</code> raises an error when the key is missing. <code>d.get(key, default)</code> returns the default instead, which shortens counting code to one line.","code":"counts = {}\nfor w in [\"a\", \"b\", \"a\"]:\n    counts[w] = counts.get(w, 0) + 1\nprint(counts)","task":"Rewrite a word count using get() so it takes one line inside the loop.","sol":"words = [\"x\", \"y\", \"x\", \"x\"]\ncounts = {}\nfor w in words:\n    counts[w] = counts.get(w, 0) + 1\nprint(counts)"},{"g":"Dictionaries","fn":"items()","t":"Looping over a dictionary","teach":"<code>.items()</code> gives the key and the value together, which is how you print a result table.","code":"totals = {\"content\": 9, \"pace\": 7}\nfor factor, total in totals.items():\n    print(f\"{factor}: {total}\")","task":"Given totals and a count of respondents, print each factor and its average.","sol":"totals = {\"content\": 9, \"pace\": 7}\nanswers = 3\nfor factor, total in totals.items():\n    print(f\"{factor}: {total / answers}\")"},{"g":"Working with text","fn":".lower() .strip()","t":"Cleaning up what was typed","teach":"<code>.lower()</code> makes text lowercase so <code>\"Ada\"</code> and <code>\"ada\"</code> compare equal; <code>.strip()</code> removes spaces and the newline at the ends. Chain them onto the input.","code":"name = input(\"Name: \").strip().lower()\nif name == \"sluta\":\n    print(\"stopping\")","task":"Read a name, clean it, and print it back in capitals with <code>.upper()</code>.","sol":"name = input(\"Name: \").strip().lower()\nprint(name.upper())","note":"The exam task says you may assume names are always typed in lowercase — but cleaning the input costs nothing."},{"g":"Working with text","fn":".split()","t":"Breaking a line into parts","teach":"<code>.split()</code> cuts a string into a list, on spaces by default or on whatever separator you pass.","code":"line = \"ada 4 5\"\nparts = line.split()\nprint(parts)\nprint(\"a,b,c\".split(\",\"))","task":"Split <code>\"4 5 3 2\"</code> into numbers and print their sum.","sol":"line = \"4 5 3 2\"\nnumbers = []\nfor p in line.split():\n    numbers.append(int(p))\nprint(sum(numbers))"},{"g":"Functions","fn":"def / return","t":"Naming a piece of work","teach":"<code>def</code> defines a function: a named block you can call repeatedly. Values go in as parameters, and <code>return</code> sends a result back. A function with no <code>return</code> gives back <code>None</code>.","code":"def average(values):\n    if len(values) == 0:\n        return 0\n    return sum(values) / len(values)\n\nprint(average([4, 5, 3]))","task":"Write a function <code>grade_text(n)</code> returning <code>\"pass\"</code> for 3 or more and <code>\"fail\"</code> otherwise, then call it.","sol":"def grade_text(n):\n    if n >= 3:\n        return \"pass\"\n    return \"fail\"\n\nprint(grade_text(4))\nprint(grade_text(2))","note":"Brookshear calls this a procedural unit — abstraction is exactly its purpose."},{"g":"Putting it together","fn":"the exam task","t":"The 2025gC survey program","teach":"The programming assignment on tag-2025gC combines almost everything above: a sentinel loop, a membership check, parallel lists, accumulation, and a formatted result. Read the shape here, then write it yourself from a blank file.","code":"# the pieces it needs\n# 1. a list of the four factors, ready in the input window\n# 2. a list of names that have answered\n# 3. a running total per factor\n# 4. while True + break on \"sluta\"\n# 5. \"Har redan svarat!\" when the name is in the list\n# 6. averages = total / number of respondents","task":"Write the whole program: keep asking for a name until <code>sluta</code>; refuse a repeat name; otherwise ask for a 1–5 grade for each factor; at the end print <code>Enkätresultat:</code> and one line per factor with its average, or <code>Ingen besvarade enkäten!</code> if nobody answered.","sol":"factors = [\"innehåll\", \"takt\", \"lärare\", \"material\"]\ntotals = [0, 0, 0, 0]\nanswered = []\n\nwhile True:\n    name = input(\"Namn: \")\n    if name == \"sluta\":\n        break\n    if name in answered:\n        print(\"Har redan svarat!\")\n    else:\n        answered.append(name)\n        for i in range(len(factors)):\n            grade = int(input(f\"Ange betyg (1-5) för {factors[i]}: \"))\n            totals[i] = totals[i] + grade\n\nif len(answered) == 0:\n    print(\"Ingen besvarade enkäten!\")\nelse:\n    print(\"Enkätresultat:\")\n    for i in range(len(factors)):\n        print(f\"{factors[i]}: {totals[i] / len(answered)}\")","note":"A program passing every test scores the full 6 points; partly working code can still score part marks."}];
window.PYGROUPS=["Input and output","Making decisions","Repeating things","Lists","Dictionaries","Working with text","Functions","Putting it together"];
